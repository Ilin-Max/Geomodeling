from setuptools import setup, find_packages

setup(
    name="geomechpy",
    version="0.1",
    packages=find_packages(),
    author="Il'in Maxim",
    description="Geomechanic calculation",
    python_requires=">=3.6",
)